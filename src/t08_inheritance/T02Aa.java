package t08_inheritance;
//사각형 처리
public class T02Aa {
	
	double areaRec(int w, int h) {
		double area = w*h;
		return area;
	}
	double lenRec(int w, int h) {
		double len = (w*2)+(h*2);
		return len;
	}
}
